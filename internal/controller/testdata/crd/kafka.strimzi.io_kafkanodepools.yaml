---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: kafkanodepools.kafka.strimzi.io
spec:
  group: kafka.strimzi.io
  names:
    kind: KafkaNodePool
    listKind: KafkaNodePoolList
    plural: kafkanodepools
    shortNames:
    - knp
    singular: kafkanodepool
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: |-
          Trimmed, envtest-only copy of the real KafkaNodePool CRD from
          github.com/strimzi/strimzi-kafka-operator (see
          external-crds/crd-strimzi-kafka-operator-v1.2.0.yaml for the full,
          vendored schema this was derived from). Only the fields
          internal/strimzi's ExtraResources actually read or write are
          modeled here.
        properties:
          apiVersion:
            type: string
          kind:
            type: string
          metadata:
            type: object
          spec:
            properties:
              replicas:
                type: integer
              roles:
                type: array
                items:
                  type: string
                  enum:
                  - controller
                  - broker
              storage:
                type: object
                properties:
                  type:
                    type: string
                    enum:
                    - ephemeral
                    - persistent-claim
                    - jbod
                  volumes:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: integer
                        type:
                          type: string
                          enum:
                          - ephemeral
                          - persistent-claim
                        size:
                          type: string
                        class:
                          type: string
                        kraftMetadata:
                          type: string
                          enum:
                          - shared
              resources:
                type: object
                properties:
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      x-kubernetes-int-or-string: true
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      x-kubernetes-int-or-string: true
                    type: object
            required:
            - replicas
            - storage
            - roles
            type: object
          status:
            properties:
              conditions:
                type: array
                items:
                  type: object
                  properties:
                    type:
                      type: string
                    status:
                      type: string
                    reason:
                      type: string
                    message:
                      type: string
                    lastTransitionTime:
                      type: string
              nodeIds:
                type: array
                items:
                  type: integer
              roles:
                type: array
                items:
                  type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
